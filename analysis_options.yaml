include: package:lints/recommended.yaml

analyzer:
  language:
    strict-casts: true
    strict-inference: true
    strict-raw-types: true

linter:
  rules:
    # Every public API element carries a doc comment. See CLAUDE.md.
    - public_member_api_docs
